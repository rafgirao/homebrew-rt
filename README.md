# Homebrew Remote Terminal

Homebrew tap for [Remote Terminal](https://github.com/rafgirao/remote-terminal).

## Install

```bash
brew install rafgirao/remote-terminal/cli
```

## Uninstall

```bash
brew uninstall cli && brew untap rafgirao/remote-terminal
```
